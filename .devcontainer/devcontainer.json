{
    "build": { "dockerfile": "Dockerfile" },

    "customizations": {
        "vscode": {
            "extensions": [
                "ms-azuretools.vscode-docker",
                "ms-python.vscode-pylance",
                "ms-python.python"
            ]
        }
    },

    "postCreateCommand": "cd data/clean && unzip clean.zip -u && cd ../.."

    "runArgs": [
        "--gpus=all"
	]
}
